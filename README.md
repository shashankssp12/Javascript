## this md is for notes, raw and rough

# Classes in JS
- They are the blueprint --> for similar kind of data
- Inheritance, super keyword, this keyword, constructor.

# Callbacks
- a callback is a fnc getting called inside another fnc as parameter
- javascript is by default asynchronous